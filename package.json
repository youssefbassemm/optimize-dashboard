{
  "name": "optimize-backend",
  "version": "1.0.0",
  "description": "Backend server for Optimize — operational dashboard for Egyptian clothing brands",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "setup": "node -e \"const fs=require('fs'),p=require('path');fs.mkdirSync('public',{recursive:true});const src=p.resolve(__dirname,'../Web/etrnll-dashboard.html');if(fs.existsSync(src)){fs.copyFileSync(src,'public/index.html');console.log('[setup] copied dashboard → public/index.html');}else{console.log('[setup] Web/etrnll-dashboard.html not found — place your dashboard HTML at public/index.html manually');}\"",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^12.8.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.5.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "nodemailer": "^8.0.5",
    "pdfkit": "^0.15.0"
  }
}
